<!DOCTYPE html>
<html>
<head>
  <title>Echoroom - Rooms</title>
  <style>
    body {
      font-family: sans-serif;
      background: #f2f2f2;
      padding: 2rem;
    }
    h1 {
      text-align: center;
      margin-bottom: 2rem;
    }
    .grid {
      display: flex;
      flex-wrap: wrap;
      gap: 1rem;
      justify-content: center;
    }
    .card {
      background: white;
      padding: 1rem 2rem;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      text-align: center;
      width: 200px;
      transition: transform 0.2s;
    }
    .card:hover {
      transform: scale(1.05);
    }
    .card a {
      text-decoration: none;
      color: #333;
      font-weight: bold;
    }
    .empty {
      text-align: center;
      color: #888;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <h1>Available Rooms</h1>
  <% if (sessions.length > 0) { %>
    <div class="grid">
      <% sessions.forEach(room => { %>
        <div class="card">
          <a href="/session/<%= room.id %>">
            <%= room.category %>
          </a>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <p class="empty">No rooms available yet.</p>
  <% } %>
</body>
</html>
